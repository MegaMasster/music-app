🎵 Aurora Music Player (React/node js + Spotify API)
    Учебный проект музыкального веб приложения, написанный на React + node js. Основная фишка — интеграция со Spotify и работа с глобальным состоянием + update skills.

⚠️ Важные нюансы (ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ ПЕРЕД ЗАПУСКОМ)

🌍 Проблема с VPN
    Так как проект использует Spotify API, а Spotify на данный момент официально не работает в РФ, приложение не сможет подгружать данные без VPN.
    Не будет работать: Поиск музыки, прослушивание музыки, отображение обложек ну и вся прочая шняга связанная с мета данными со спотифая.
    Включите VPN (желательно регион Европа или США) перед запуском проекта.

🧩 Особенности этого сайта (Iframe)
    В проекте используется Spotify Embed (Iframe). Это решение было выбрано из-за специфики работы Spotify API:
    Отсутствие прямых аудио-файлов (MP3/WAV): Spotify API не отдает прямые ссылки на аудио-потоки или файлы. А для полноценного управления воспроизведением через собственный интерфейс (Play/Pause/Seek) требуется использование Web Playback SDK, который доступен только пользователям с Premium-подпиской и требует сложной авторизации. Использование Iframe — единственный способ легально и бесплатно воспроизводить музыку в проекте.
    Управление из кода: В силу ограничений безопасности браузеров (CORS), я не могу программно управлять содержимым айфрейма(управлять музыкой) , (например, нажать на паузу из нашего React-кода или изменить громкость). Наш контроллер — это визуальная оболочка, которая передает ID трека во фрейм.
    Автовоспроизведение (Autoplay): Современные браузеры блокируют звук при автоматической загрузке. Чтобы музыка заиграла после выбора трека, пользователю необходимо проявить «активность» на странице (хотя бы один клик).
    Синхронизация: Плеер вынесен в глобальный контроллер. Это гарантирует, что музыка не прерывается при переходах между страницами приложения.

🛠 Архитектурные решения
    Дебаунс поиска: Чтобы не спамить запросами к Spotify при каждом введенном символе, и что бы мгновенно отображался результат реализована задержка (Debounce). 
    Запрос уходит только через 320мс после того, как пользователь закончил ввод.
    Пагинация результатов: Чтобы не перегружать интерфейс, 18 найденных треков разбиваются на страницы по 6 штук.
    Глобальный плеер-синглтон: Плеер отрендерен в корне приложения. Это позволяет менять треки из любой части программы без перерендера всего плеера.
    Оптимизация рендеринга: Вместо того чтобы рисовать 6 тяжелых плееров в результатах поиска, мы рисуем легкие карточки, а тяжелый iframe подгружается только один раз в контроллере.



# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Babel](https://babeljs.io/) (or [oxc](https://oxc.rs) when used in [rolldown-vite](https://vite.dev/guide/rolldown)) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for information on how to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project.
